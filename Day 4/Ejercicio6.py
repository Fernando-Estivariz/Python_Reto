def calcular_area_del_triangulo(base, altura):
    area = (base * altura) / 2
    return area

base = 5
altura = 8
area = calcular_area_del_triangulo(base, altura)
print(f"El área del triángulo con base {base} y altura {altura} es {area}.")
